=link_to_back
.page-header
  %h1=@country.name
-if @country.edit?(@current_user)
  .btn-toolbar
    =link_to edit_country_path(@country),class: 'btn btn-primary' do
      =fa_icon 'edit'
      Редактировать
    =link_to @country,class: 'btn btn-danger',method: :delete, data: {confirm: t('helpers.sure')} do
      =fa_icon 'trash-o'
      Удалить
%h2 Фильмы
-if @films.blank?
  .alert.alert-info Фильмов не найдено
-else
  .list-group
    -@films.each do |film|
      =render 'films/film', film: film
  =paginate @films
