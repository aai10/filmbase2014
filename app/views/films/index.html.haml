.page-header
  %h1
    Фильмы
    -if Film.add?(@current_user)
      =link_to new_film_path,class: 'btn btn-primary' do
        =fa_icon 'plus'
.list-group
  -@films.each do |film|
    =render 'film', film: film
=paginate @films

